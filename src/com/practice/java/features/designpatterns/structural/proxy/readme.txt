-- Concepts --
1. Acts as an interface to other object
2. provides an object that acts as a substitute for a real service object used by a client
3. Intercepts the calls to actual instance and give the object after applying rules.
