-- Concepts --
1. Connecting new code to legacy code (use adapters to do it)
2. Converting clients request to the format we need.
3. Java API: Arrays -> Lists (arrays are legacy, lists are new. Collections has adapters to covert arrays to lists)
, Streams
